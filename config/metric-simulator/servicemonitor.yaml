apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: metric-simulator
  namespace: monitoring
  labels:
    release: prometheus
spec:
  namespaceSelector:
    matchNames:
    - default
  selector:
    matchLabels:
      app: metric-simulator
  endpoints:
  - port: http
    path: /metrics
    interval: 15s
