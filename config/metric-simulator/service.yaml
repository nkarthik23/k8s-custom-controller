apiVersion: v1
kind: Service
metadata:
  name: metric-simulator
  namespace: default
  labels:
    app: metric-simulator
spec:
  selector:
    app: metric-simulator
  ports:
  - name: http
    port: 8080
    targetPort: 8080
