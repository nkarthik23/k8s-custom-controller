apiVersion: v1
kind: Service
metadata:
  name: inference-server
  namespace: default
  labels:
    app: inference-server
spec:
  selector:
    app: inference-server
  ports:
  - name: http
    port: 8080
    targetPort: 8080
