apiVersion: apps.yourdomain.dev/v1
kind: MetricAutoscaler
metadata:
  labels:
    app.kubernetes.io/name: k8s-custom-controller
    app.kubernetes.io/managed-by: kustomize
  name: metricautoscaler-sample
spec:
  # TODO(user): Add fields here
