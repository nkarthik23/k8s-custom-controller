apiVersion: apps.yourdomain.dev/v1
kind: MetricAutoscaler
metadata:
  name: queue-autoscaler
  namespace: default
spec:
  targetDeployment: inference-server
  prometheusQuery: "queue_depth{container='inference-server'}"
  threshold: "5"
  minReplicas: 1
  maxReplicas: 2