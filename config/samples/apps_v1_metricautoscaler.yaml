apiVersion: apps.yourdomain.dev/v1
kind: MetricAutoscaler
metadata:
  name: queue-autoscaler
  namespace: default
spec:
  targetDeployment: metric-simulator
  prometheusQuery: queue_depth
  threshold: "50"
  minReplicas: 1
  maxReplicas: 5
